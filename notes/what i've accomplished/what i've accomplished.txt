3/18:
- Created a minefield array with customizable width and height
- Created a function that randomly adds mines to the minefield
- Created a tile object that holds its index (will be very useful for communicating with game later on)
- Successfully drew the minefield to the screen
- Made decent progress on the function that'll scan for mines to decide each of the tiles' numbers

To learn version control:
https://www.youtube.com/watch?v=ekOp35B1Bdg


LONG TERM PLAN:
1. Create a minefield array that's fully complete (0-9)
2. Create an empty version that the player will play on
	- Have it check the complete version every click and update it
3. When you click on a 0, reveal all of its adjacent tiles (diagonals too) and numbers that border them
4. When you click for the first time, move the mine away if you click on it
	- Move mines within a 5x5 square radius (as an example) of where you first click